apiVersion: v1
kind: Service
metadata:
  namespace: stonehaven-staging
  name: farmrun-item-service
  labels:
    environment: staging
    app: farmrun-item-service
spec:
  selector:
    app: farmrun-item-service
  type: ClusterIP
  ports:
    - name: farmrun-item-service
      protocol: TCP
      port: 8080
      targetPort: 8080
  